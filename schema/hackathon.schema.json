{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/shreyansigheware/hackboard/schema/hackathon.schema.json",
  "title": "Hackathon",
  "description": "One competitive, time-boxed event with a submission deadline and a prize or certificate. Hackathons, ideathons, case competitions, CTFs, datathons and company challenges all qualify.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id", "name", "organiser", "scope", "mode", "themes",
    "starts", "ends", "url", "source_url", "first_seen", "last_seen"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Stable slug. Normalised name + start month, e.g. hackx-2026-08. Must not change once published — the URL and any shortlist depend on it."
    },
    "name": { "type": "string", "minLength": 2, "maxLength": 160 },
    "organiser": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "type"],
      "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 120 },
        "type": { "enum": ["company", "university", "community", "government"] }
      }
    },
    "scope": {
      "enum": ["global", "india", "city"],
      "description": "city means the event is tied to one city; the city field is then required."
    },
    "city": {
      "type": "string",
      "maxLength": 80,
      "description": "Required when scope is city. Omitted otherwise."
    },
    "mode": { "enum": ["online", "in-person", "hybrid"] },
    "themes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "uniqueItems": true,
      "items": {
        "enum": ["ai", "web3", "fintech", "healthtech", "cybersecurity", "open-source", "sustainability", "hardware", "other"]
      }
    },
    "starts": { "type": "string", "format": "date", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "ends": { "type": "string", "format": "date", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "registration_closes": {
      "type": ["string", "null"],
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "null when the source does not state one. Never guessed — a wrong deadline is the most damaging error this dataset can make."
    },
    "prize_inr": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Total prize pool in INR. null when there is no cash prize or the source does not state one. 0 means explicitly no cash prize."
    },
    "team": {
      "type": "object",
      "additionalProperties": false,
      "required": ["min", "max"],
      "properties": {
        "min": { "type": "integer", "minimum": 1 },
        "max": { "type": ["integer", "null"], "minimum": 1 }
      },
      "description": "min 1 means solo is allowed. max null means no stated upper limit."
    },
    "eligibility": { "enum": ["open", "students", "women", "freshers"] },
    "url": { "type": "string", "format": "uri", "pattern": "^https?://" },
    "source_url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://",
      "description": "Where this row was found. Required so any claim can be checked."
    },
    "first_seen": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "last_seen": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Last daily run that still found this event at its source. A stale last_seen is how a dead listing is spotted."
    },
    "locked": {
      "type": "boolean",
      "default": false,
      "description": "Corrected by hand. The merge step must not overwrite a locked row."
    },
    "synthetic": {
      "type": "boolean",
      "default": false,
      "description": "Generated for the stress test. Must never be true in data/hackathons.json."
    }
  },
  "allOf": [
    {
      "if": { "properties": { "scope": { "const": "city" } }, "required": ["scope"] },
      "then": { "required": ["city"] },
      "else": { "not": { "required": ["city"] } }
    }
  ]
}
